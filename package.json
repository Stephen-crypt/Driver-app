{
  "name": "gera",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "test:functions": "deno test --allow-read --allow-run --config supabase/functions/deno.json supabase/functions/",
    "verify": "pnpm -r typecheck && pnpm -r test && pnpm test:functions",
    "bundle:core": "node scripts/bundle-core.mjs",
    "e2e": "node scripts/e2e-trip-loop.mjs",
    "simulate": "node scripts/simulate-drivers.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
