# Expo's React Native toolchain assumes a flat node_modules: Metro resolves many
# transitive Expo internals that are never declared as direct dependencies, and
# pnpm's default symlinked layout hides them. Without this the apps install fine
# and typecheck fine but cannot produce a bundle.
node-linker=hoisted
